language: python
python:
  - "3.5"
install:
  - pip install python-coveralls


script:
  - make --doctest-modules --pep8 coveralls -v --cov coveralls --cov-report term-missing

after_success:
  - coveralls